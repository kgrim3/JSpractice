function truncateString(str, num) {
  if (str.length <= num) {
    return str;
  } 
  else {
      str = str.substr(0, num);
      return str + "...";   
  }
}
truncateString("A-tisket a-tasket A green and yellow basket", 8);
//return a truncated string based on the given num as an index
